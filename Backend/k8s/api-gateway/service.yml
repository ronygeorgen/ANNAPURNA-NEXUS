apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  selector:
    app: api-gateway
  type: LoadBalancer  # This makes it externally accessible
  ports:
  - port: 8005
    targetPort: 8005